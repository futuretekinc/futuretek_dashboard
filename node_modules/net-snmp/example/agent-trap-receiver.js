
var snmp = require ("net-snmp");

var agent = snmp.createAgent ();

agent.on ("close", function () {
	console.log ("closed");
});

agent.on ("error", function (error) {
	console.error (error.toString ());
});

agent.on ("trap", function (trap) {
	console.dir (trap);
});

agent.listen ();
